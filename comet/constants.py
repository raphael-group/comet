# Mappings from program arguments to chars passed into C program as flags
EXACT, DENDRIX, BINOM = 'e', 'd', 'b'
weight_function_chars = {
    'exact' : EXACT,
    'dendrix' : DENDRIX,
    'binom' : BINOM
    }
